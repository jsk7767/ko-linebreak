---
name: ko-linebreak
description: 한국어 문서·웹페이지·PPT의 줄바꿈 교정. "줄바꿈 정리/점검해줘", 단어 중간 꺾임·외톨이 글자·의미 안 맞는 줄바꿈 지적, 사용자가 개행 예시를 보여줄 때 사용. keep-all CSS + 문장 단위 + 절 단위 호흡 개행. 줄바꿈 작업에서 문구는 절대 수정하지 않는다.
---

# 한국어 줄바꿈 교정 (ko-linebreak)

한국어 텍스트가 "기록입니/다"처럼 단어 중간에서 꺾이거나, 의미와 무관하게 줄이 갈리는 문제를 교정한다.
(유래: 2026-09-01 네일샵 홈페이지 조판 작업 — 사용자가 스크린샷·전후 예시로 형식을 직접 시연하며 확립한 규칙.)

## ★ 의도 파악 규칙 (사용자 수정요청 패턴에서 학습 — 최우선)

1. **입력 원문의 개행은 조판 의도다.** 사용자·화자가 개행해서 준 텍스트(편지·카피·시연 예시)를 문단으로 합쳐 흘리지 말 것. 그 개행 구조를 그대로 보존하는 것이 기본값.
2. **예시가 규칙보다 우선.** 사용자가 전/후 대비나 재개행본을 보여주면 그것이 스펙이다. 예시에서 규칙을 역추출해 **같은 패턴의 모든 블록에 일반화**할 것 — 보여준 블록만 고치고 끝내지 않는다.
3. **줄바꿈 작업에서 문구 수정 금지.** 단어·어미·어순·수치 일체 불변, 오직 개행 위치만 바꾼다. (줄바꿈 지적을 문체 지적으로 확대 해석했다가 교정받은 전례 있음.)
4. **끝 단계까지 한 번에.** 사용자 지적은 문단 → 문장 → 절 순으로 정제되며 반복된다. 처음부터 3단계(절 단위)까지 적용해 재요청을 만들지 말 것.
5. 완료 보고는 사용자 시연과 같은 형식(개행된 인용 블록)으로 보여줄 것 — 결과를 눈으로 확인시킨다.

## 3단계 규칙

### 1단계 — CSS 기본기 (웹, 기계적 필수)
```css
body{word-break:keep-all;overflow-wrap:break-word}   /* 어절 단위로만 줄바꿈 */
p,li,dd,dt,figcaption,summary,small,span{text-wrap:pretty}  /* 외톨이 글자 방지 */
h1,h2,h3{text-wrap:balance}                           /* 제목 균형 분할 */
```
브라우저 기본값은 CJK를 글자 단위로 꺾는다. 한국어 페이지라면 무조건 넣는다.

### 2단계 — 문장 단위 개행
여러 문장으로 된 **소개문·캡션·리드·FAQ 답변·감성 문단(편지·스토리)**은 자동 wrap에 맡기지 않고
문장 경계마다 `<br>`(HTML) 또는 개행(텍스트/PPT)을 넣는다.
```
손님들이 리뷰에 가장 많이 남긴 말이 "원하는 디자인을 잘해줘요"예요.<br>
그 말의 증거들을 모았습니다. 전부 실제 시술 기록이에요.
```
- 짧은 문장 2개는 한 줄에 묶어도 된다(둘째 줄처럼). 기준은 소리 내어 읽을 때의 호흡.
- 본문형 긴 산문(블로그 글 등)에는 적용하지 않는다 — 디스플레이성 블록 전용.

### 3단계 — 절 단위 호흡
긴 문장은 쉼표·연결어미에서 한 번 더 끊는다. 감성 문단(편지·스토리·히어로 카피)에서는
길이와 무관하게 **주제부("~하는 건,")·연결부("~했고,") 뒤에서 적극적으로** 끊는다.
```
그런데도 가게가 돌아가는 건,
다녀간 분들이 곁의 사람을 한 명씩 데려와 주신 덕분입니다.

2023년 가을엔 손님이 늘어 더 넓은 지금 자리로 옮겨 왔고,
2025년부터는 후배 네일리스트들을 1:1 수강 클래스에서 만나고 있어요.
```
정보성 행(주소·메뉴·표 안 텍스트)은 절 분리를 하지 않는다 — 검토 후보로만 취급.

### 4단계 — 모바일 의미 청킹 (좁은 화면 재접힘 대비)
수동 `<br>`는 넓은 화면 기준이라, 모바일에서 한 줄이 다시 접히며 "문을/여는" 같은 의미 파괴가 재발한다.
히어로·헤드라인 등 핵심 디스플레이 텍스트는 **의미 덩어리를 `<span class="nb">`(`.nb{white-space:nowrap}`)로 묶어**
줄바꿈이 덩어리 사이에서만 일어나게 한다.
```html
<span class="nb">반월당역 지하상가에서</span> <span class="nb">예약제로 문을 여는</span> <span class="nb">네일샵이에요.</span>
```
덩어리는 3~10자 구 단위(너무 길면 모바일에서 overflow — 최장 덩어리가 최소 화면폭보다 짧아야 함).

### 부수 규칙
- 수치·출처 병기 "(2026년 8월, ○○ 기준)"은 본문 문장에 넣지 말고 별도 `<small>` 라인/표로 분리.
- 문장 속 나열("그 외: A · B · C")은 개행이 아니라 표·리스트 구조로 옮긴다.
- JS로 렌더되는 텍스트는 `white-space:pre-line` + 상태 문자열 `\n` (innerHTML `<br>` 주입 지양).
- JSON-LD 등 기계용 텍스트에는 `<br>` 금지(가시 텍스트만).

## 적용 대상별 방법

| 대상 | 방법 |
|---|---|
| HTML/웹 | 1단계 CSS + 2·3단계 `<br>` |
| Markdown/플레인 | 2·3단계를 실제 개행으로. MD 줄바꿈 유지는 줄 끝 공백 2개 또는 `<br>` |
| PPTX | `py -3 -m pip install python-pptx` 후 텍스트프레임 순회 — 2·3단계 지점에 소프트 리턴(`\n`) 삽입. 제목은 두 줄 길이 균형(balance) |
| DOCX/HWP | docx는 python-docx 동일 원리. HWP는 텍스트 추출 → 수정본 전달 |
| 참고 | 구문 '허용 지점' 자동화는 google/budoux(ja·zh·th, 한국어 모델 없음 — 2026-09 재확인)·Chrome `word-break:auto-phrase`. 이 스킬의 `kolb.chunk_phrases` 가 한국어 조사·어미 규칙으로 같은 일을 한다. 문장 경계는 hyunwoongko/kss 가 있으면 자동 사용 |

## 절차 (v2 — 도구 3종)

```bash
S=~/.claude/skills/ko-linebreak/scripts
py -3 $S/check.py FILE.html                     # 1) 정적 점검: CSS·미개행·줄머리 조사·nb 길이·금지위치 (--json 가능)
py -3 $S/fix.py FILE.html --sentences --clauses 45 --nb "h1,.lede" --css   # 2) 미리보기(diff)
py -3 $S/fix.py FILE.html --sentences --clauses 45 --nb "h1,.lede" --css --write   #    백업 후 적용
py -3 $S/render_check.py FILE.html --widths 390,768,1280 [--outline shot.png]     # 3) Chromium 실측: 어절 중간 꺾임·nb 갈림/넘침·자동 wrap 줄머리·외톨이
```

1. `check.py` 로 필수 위반을 본다. 문장 경계는 kss 가 설치돼 있으면 kss, 없으면 내장 정규식(종결부호+인용부호, 소수점·전화번호 보호).
2. `fix.py` 는 **문구를 바꾸지 않는다** — `<br>`·`<span class="nb">`·CSS만 넣고, 적용 전에 순수 텍스트가 같은지 스스로 검증한다(다르면 중단). `--nb` 는 히어로·리드 같은 핵심 디스플레이 요소에만 준다. 덩어리 규칙: 쉼표·강한 조사(에서·으로·에·와·도) 뒤는 6자 이상이면 끊고, 약한 조사(을·를·은·는·이·가·의) 뒤는 8자 이상일 때만, 숫자 어절(16년·28,000원)은 뒤 명사와 붙인다. 최장 12자(공백·부호 제외).
3. 자동 적용 뒤 **의미 경계는 사람이 한 번 본다** — 특히 `[절]`. 관형형 "~는 + 명사"가 갈렸으면 손으로 합친다.
4. `render_check.py` 가 진짜 판정자다. 정적 점검이 못 보는 것(글꼴 크기에 따른 nb 넘침, `display:flex` 부모 안의 span 분리, 자동 wrap 으로 조사가 줄머리에 오는 것)을 390/768/1280 에서 잰다. `--outline` 으로 nb 경계(점선)와 `<br>`(⏎)를 찍은 스크린샷을 남겨 사용자에게 보여준다. `--font-scale 1.25` 로 큰 글씨 설정도 확인.
5. 결과를 개행된 인용 블록으로 사용자에게 보여주고, 제외한 블록은 사유 1줄.

### 함정 (실측에서 나온 것)
- `display:flex` 인 `<summary>`·버튼 안에 `<span class="nb">` 를 넣으면 span 이 별도 flex 아이템이 되어 줄이 뒤집힌다 → 텍스트 전체를 `<span>` 하나로 감싼 뒤 그 안에서 nb.
- nb 안에 `<em>` 같은 인라인 자식이 있으면 `getClientRects()` 가 여러 개다 — 세로 위치가 다를 때만 '갈림'으로 본다(render_check 반영).
- 정적 nb 길이 기준(18자)은 참고다. 15.5px 본문에서 24자 덩어리는 390px 을 넘쳤고, 12.5px small 에서 26자는 안 넘쳤다. 넘침은 실측으로만 확정.
- `text-wrap: pretty/balance` 는 브라우저 지원이 다르다(caniuse 확인). 미지원 환경의 외톨이는 nb 청킹이 유일한 통제 수단이고, 제목 균형은 adobe/balance-text·nytimes/text-balancer 같은 JS 폴백이 있다.
- 파이썬 re 는 가변폭 lookbehind 를 거부한다 — 연결어미 매치는 어미를 매치에 포함하고 경계는 `m.end()` 로 잡는다.
